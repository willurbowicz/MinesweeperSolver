A python program designed to solve a game of minesweeper in the browser.

Captures an image of the monitor(s) and dynamically determine board position based on landmarks

Pick a square to start the game

Calculate which tiles, if any, are guaranteed to have a flag based on the current tile configuration

Calculate which tiles, if any, are guaranteed to be safe based on the current tile configuration

If all else fails, guess.